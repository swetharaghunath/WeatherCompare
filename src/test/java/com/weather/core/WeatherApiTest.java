package com.weather.core;

public class WeatherApiTest extends BaseTest{
	
	/*
	 * WebElement webElement=driver.findElement(By.tagName("pre"));
	 * WeatherApiResponse weatherApiResponse=new WeatherApiResponse();
	 * WeatherApiResponse ExpectedString=weatherApiResponse; // /
	 * Assert.assertTrue(webElement.getText().equals(ExpectedString));
	 */
	
}
